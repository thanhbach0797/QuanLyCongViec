<Window x:Class="TaskManagementApp.Views.EmployeeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Task Management - Employee" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Menu bên trái -->
        <StackPanel Grid.Column="0" Background="#2E2E2E">
            <TextBlock Text="Employee Menu" FontSize="18" FontWeight="Bold" 
                       Foreground="White" Margin="10" HorizontalAlignment="Center"/>
            <Button Content="Dashboard" FontSize="14" Margin="10,5" Background="#0078D4" 
                    Foreground="White" Height="40" Command="{Binding NavigateToDashboardCommand}"/>
            <Button Content="Công việc của tôi" FontSize="14" Margin="10,5" Background="#0078D4" 
                    Foreground="White" Height="40" Command="{Binding NavigateToMyTasksCommand}"/>
            <Button Content="Thông tin Cá nhân" FontSize="14" Margin="10,5" Background="#0078D4" 
                    Foreground="White" Height="40" Command="{Binding NavigateToProfileCommand}"/>
            <Button Content="Logout" FontSize="14" Margin="10,5" Background="#FF4444" 
                    Foreground="White" Height="40" Command="{Binding LogoutCommand}"/>
        </StackPanel>

        <!-- Nội dung chính -->
        <ContentControl Grid.Column="1" Margin="20" Content="{Binding}">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <!-- Mặc định hiển thị Dashboard -->
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!-- Tiêu đề -->
                                    <TextBlock Grid.Row="0" Text="Dashboard" FontSize="24" FontWeight="Bold" 
                                               Foreground="#2E2E2E" Margin="0,0,0,10"/>

                                    <!-- Bộ lọc -->
                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="Lọc theo Dự án:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <ComboBox ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProjectFilter}" 
                                                  DisplayMemberPath="Name" Width="150" FontSize="14" Margin="0,0,10,0"/>
                                        <Button Content="Lọc" Command="{Binding FilterDashboardCommand}" 
                                                Width="80" Height="30" Background="#0078D4" Foreground="White"/>
                                    </StackPanel>

                                    <!-- Widget thống kê -->
                                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                                        <Border Background="#0078D4" Margin="10" Padding="20" CornerRadius="5">
                                            <StackPanel>
                                                <TextBlock Text="In Progress" Foreground="White" FontSize="16"/>
                                                <TextBlock Text="{Binding InProgressCount}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#28A745" Margin="10" Padding="20" CornerRadius="5">
                                            <StackPanel>
                                                <TextBlock Text="Completed" Foreground="White" FontSize="16"/>
                                                <TextBlock Text="{Binding CompletedCount}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#DC3545" Margin="10" Padding="20" CornerRadius="5">
                                            <StackPanel>
                                                <TextBlock Text="Overdue" Foreground="White" FontSize="16"/>
                                                <TextBlock Text="{Binding OverdueCount}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding CurrentView}" Value="MyTasks">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <!-- Tiêu đề và nút Thêm Công việc -->
                                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                                <TextBlock Text="Công việc của tôi" FontSize="24" FontWeight="Bold" Foreground="#2E2E2E"/>
                                                <Button Content="Thêm Công việc" Command="{Binding AddTaskCommand}" 
                                                        FontSize="14" Margin="10,0,0,0" Background="#28A745" 
                                                        Foreground="White" Width="120" Height="30" HorizontalAlignment="Right"/>
                                            </StackPanel>

                                            <!-- Bộ lọc -->
                                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                                <TextBlock Text="Lọc theo Dự án:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <ComboBox ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProjectFilter}" 
                                                          DisplayMemberPath="Name" Width="150" FontSize="14" Margin="0,0,10,0"/>
                                                <TextBlock Text="Lọc theo Trạng thái:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <ComboBox ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatusFilter}" 
                                                          Width="150" FontSize="14" Margin="0,0,10,0"/>
                                                <Button Content="Lọc" Command="{Binding FilterTasksCommand}" 
                                                        Width="80" Height="30" Background="#0078D4" Foreground="White"/>
                                            </StackPanel>

                                            <!-- Bảng danh sách công việc -->
                                            <DataGrid Grid.Row="2" ItemsSource="{Binding FilteredTasks}" AutoGenerateColumns="False" 
                                                      CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="Task ID" Binding="{Binding TaskId}" Width="80"/>
                                                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="150"/>
                                                    <DataGridTextColumn Header="Project" Binding="{Binding ProjectId}" Width="100"/>
                                                    <DataGridTextColumn Header="Assigned To" Binding="{Binding AssignedTo}" Width="120"/>
                                                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                                                    <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate}" Width="120"/>
                                                    <DataGridTemplateColumn Header="Action" Width="150">
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Button Content="View" Margin="2" Background="#0078D4" Foreground="White"
                                                                            Command="{Binding Path=DataContext.ViewTaskCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                                            CommandParameter="{Binding}"/>
                                                                    <Button Content="Edit" Margin="2" Background="#FFA500" Foreground="White"
                                                                            Command="{Binding Path=DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                                            CommandParameter="{Binding}"/>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </Grid>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</Window>