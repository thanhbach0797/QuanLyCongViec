<Window x:Class="TaskManagementApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:helpers="clr-namespace:TaskManagementApp.Helpers"
        Title="Đăng nhập" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="Ứng dụng Quản lý Công việc" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#2E2E2E"/>

        <!-- Tên đăng nhập -->
        <TextBlock Grid.Row="1" Text="Tên đăng nhập" FontSize="14" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                 FontSize="14" Padding="5" Margin="0,0,0,10" BorderBrush="#CCCCCC"/>

        <!-- Mật khẩu -->
        <TextBlock Grid.Row="3" Text="Mật khẩu" FontSize="14" Margin="0,0,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" FontSize="14" Padding="5" 
                     Margin="0,0,0,20" BorderBrush="#CCCCCC">
            <i:Interaction.Behaviors>
                <helpers:PasswordBoxBindingBehavior Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </i:Interaction.Behaviors>
            <PasswordBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
            </PasswordBox.InputBindings>
        </PasswordBox>

        <!-- Thông báo lỗi -->
        <TextBlock Grid.Row="5" Text="{Binding ErrorMessage}" Foreground="Red" 
                   FontSize="12" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Nút Đăng nhập -->
        <Button Grid.Row="5" Content="Đăng nhập" Command="{Binding LoginCommand}" 
                Width="100" Height="40" Background="#0078D4" Foreground="White" 
                FontSize="14" HorizontalAlignment="Center" Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="1" CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>